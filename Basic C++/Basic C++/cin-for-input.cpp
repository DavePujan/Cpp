#include <iostream>
using namespace std;

int main()
 {
//     int n;
//     cin >> n;
//     cout << "entered number is "<< n << endl;

    int a= cin.get();  // try this by entering enter or space, output will be 32 or 10, so we can say that by  using cin.get we can access enter,tab,space any thing but using cin we are not able to read enter, space or tab.
    cout << "entered number is:" << a << endl;
    
    return 0;
}